// Condicional encadeada

// Criaçãp da variável media
double media = 8;

// Estrutura condicional encadeada
if (media >= 6)
{
    Console.WriteLine("Você foi aprovado(a)!");
}

else if (media >= 5)
{
    Console.WriteLine("Você está em recuperação!");
}

else
{
    Console.WriteLine("Você foi reprovado(a)!");
}
